node_modules/
dist/
*.tsbuildinfo
.env
.env.local
.DS_Store
.expo/
*.log

# Expo prebuild output (continuous native generation — regenerate with `npx expo prebuild`)
packages/app/ios/
packages/app/android/

# Claude Code — local/private files
# (shared config stays tracked: CLAUDE.md, .claude/agents/, .claude/settings.json)
.claude/settings.local.json
**/.claude/settings.local.json
CLAUDE.local.md
**/CLAUDE.local.md
.claude/memory/
.claude/plans/

# test artifacts
coverage/

# Working design notes, not yet part of the tracked handoff bundle in design/.
# NOTE: packages/app/src/theme.ts cites "Otrolado Design System.md" as the
# source of its tokens, so this is a temporary parking spot — the doc belongs
# in design/ (tracked) once the Cobalt token set is settled.
Otrolado Design System.md
